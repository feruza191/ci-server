services:
    db: &postgres
        container_name: postgres
        image: postgres:11.5
        env_file:
            - .env
            - .env.local
        volumes:
            - ./server/pgdata:/var/lib/postgresql/data
        ports:
            - '5432:5432'
        restart: unless-stopped

volumes:
    postgres:
